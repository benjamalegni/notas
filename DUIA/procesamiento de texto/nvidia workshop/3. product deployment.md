after having a trained network. now what?

application bandwidth = cost

they have 2 unique inference stages
- prefill: processes all the request's input tokens to calculate intermediate states
- decode: sequentially generates output tokens, updating the intermediate states calculated during the prefill stage for each new token
![[Pasted image 20260817122144.png]]

there's more to an application than just the model

not all models respond to the same way to knowledge distillation, pruning and quantization
there's an increased importance of pruning and quantization

### post training quantization
![[Pasted image 20260817122500.png]]
the results are speedup and throughput

not all neural network mechanisims quantize well

### reasons for loss of accuracy
outlier in the tensor:
- BERT, inception v4
- solnt: clip. tighten the range, use bits more efficiently
not enough precision in quantized representation
- ex: int8 for mobilenet v1
- ex: int4 for resnet50
- solnt: train/fine tune for quantization

## quantization tools
![[Pasted image 20260817123453.png]]
![[Pasted image 20260817124039.png]]

## pruning
- reduced memory bandwidth
- reduced memoty footprint
- acceleration

fine-grained structured sparsity for tensor cores:
- 50% fine-grained sparsity
- 2:4 pattern: 2 values out of each contiguous block of 4 must be 0

address the 3 challenges:
- accuracy: maintains accuracy of the original, unpruned network
	- medium sparsity level (50%), fine-grained
- training: a recipe shown to work across tasks and networks
- speedup:
	- specialized tensor core support for sparse math
	- structured: lends itself to efficient memory utilization

### recipe for 2:4 sparse network training
1. train or obtain a dense network
2. prune for 2:4 sparsity
3. repeat the original training procedure
	1. same hyper-parameters as in step-1
	2. initialize to weights from step 2
	3. maintain the 0 pattern from step-2: no need to recompute the mask


APEX AUTOMATIC SPARSITY: ASP
![[Pasted image 20260817125011.png]]
## product quantization
![[Pasted image 20260817125152.png]]

extreme model compression: training with quantization noise

## knowledge distillation
is basically a bigger model teaching a smaller one to behave like him

## model code efficiency
nvidia nim optimized inference microservices

code quality is key
![[Pasted image 20260817130003.png]]

### optimizing inference with tensorrt-llm
parallelism:
- split LLM workload across multiple GPUs

usage:
- convert checkpoint to TRT-LLM format
	- common format for TRT-LLM to ingest from any framework
	- TRT-LLM format HF format
	- predefined from popular frameworks and supported models
- build tensorRT engines
	- creates an optimized artifact ready to deploy
	- AoT compiled for verification
	- high-level API (LLM.\*) handles conversion and building
- execute
	- tensorRT-LLM runtime handles TRT, custom plugins and runtime optimizations like inflight batching, KV caching, etc.
![[Pasted image 20260817130442.png]]
![[Pasted image 20260817130534.png]]

### LoRA and customization
- LoRa and prompt tuned models are supported in TRT-LLM
- support multiple customers with a single model
- dynamically swap LoRA's at runtime
- SLoRA/LoRAx caching adapters on device
- base model can be quantized for memory savings
	- QLoRA in progress

KV-cache reusage
- allows for interactive/turn based systems an system promps![[Pasted image 20260817131107.png]]
inflight batching:
- maximizing GPU utilization during LLM serving
- replaces completed requests in the batch
	- evicts requests after EoS and inserts a new request
- ![[Pasted image 20260817131246.png]]
KV cache optimizations
- paged and quantized KV cache improved mamory consumption and utilization
- allows for reduced memoty consumption of KV cache
- allocates memory on demand
quantized KV improves memory consumption and perf
- reduces kv cache elements from 16b to 8b or less
- reduces memoty transfer improving performance
- supports INT8/FP8 kv caches

![[Pasted image 20260817133120.png]]

### optimized attention
custom implementations for attention

- custom optimized cuda kernels for attention
	- similar to flashattentionv2
- optimized for A100 and H100
- kernels for encoder and decoder, as well as context and prefill
- supports MHA, MQA, GQA
- ![[Pasted image 20260817133352.png]]

### multi-gpu multi-node
- supports tensor and pipeline parallelism
- allows for running very large models (tested up to 530B)
- supports multi-gpu (single node) and multi-node
- tensorRT-LLM handles communication between GPUs
- examples are parametrized for sharding across GPUs
- add expert paralelle

# TRT-LLM custom model support
- define model.py script outlining the architecture
- write convert_checkpoint.py which converts the checkpoint to the TRT LLM format
	- optional: quantize your model
- build the engine using trtllm-build
- optional: write example/ to run and test your engine

### inefficiency limits innovation
- ease of use
- cost of deployment
- ecosystem tools

triton inference server:
![[Pasted image 20260817141812.png]]
- features
	- concurrent model execution
	- CPU model inference execution
	- metrics
	- custom backend
	- model ensemble
	- dynamic batching
	- multiple model format support
	- CMake build
	- streaming API

steps:
- create model repository
- launch server
- launch clients
- request

4 components in pipeline:
1. preprocessing: tokenize the input
2. tensorRT-LLM: inference engine
3. post-processing: decoding text output
4. ensemble: contains the I/O and sequence of stages
![[Pasted image 20260817142414.png]]

using the tritonserver command, build and run your tensorRT-LLM engines without model parallelism
or use scripts/launch_triton_server.py to set the number of workers needed when the tensorRT-LLM engines are built with parallelism

# application
is typically composed of many components
![[Pasted image 20260817143702.png]]

RIVA
e2e multimodal conversational AI services. pre-trained SOTA models-100000 hours of DGX

retrain models for your domain using NeMo and TAO toolkit

